# Bandit Level 21 → Level 22

### 🔑 Objective
Reverse engineer an automated script file using targeted MD5 hashing functions to determine the exact temporary filename chosen by a background task.

### 🛠️ Core Command Blueprint
```bash
# Step 1: Read the tracking application configuration properties
cat /usr/bin/cronjob_bandit23.sh

# Step 2: Calculate the strict MD5 name hash generated by the system user footprint string
echo "bandit23" | md5sum

# Step 3: Read the hashed temporary log container file matching that hash signature
cat /tmp/8ca319486bf10593e57ced2240212d20

# Password: tRae0UfB9v0UzbCdn9cY0gQnds9GF58Q
